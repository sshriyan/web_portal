spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

turbine:
  aggregator:
    clusterConfig: SAMPLE-HYSTRIX-AGGREGATE
  appConfig: SAMPLE-HYSTRIX-AGGREGATE